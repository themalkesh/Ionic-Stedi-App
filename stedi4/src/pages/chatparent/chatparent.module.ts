import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ChatparentPage } from './chatparent';

@NgModule({
  declarations: [
   ChatparentPage,
  ],
  imports: [
    IonicPageModule.forChild(ChatparentPage),
  ],
})
export class ChatparentPageModule {}
